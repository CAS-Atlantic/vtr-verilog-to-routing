#include <iostream>
#include <fstream>

// Declaring the custom functions or classes related to I/O operations

// Function to open a file in append mode
void openFile(std::ofstream& file, const std::string& filename);
